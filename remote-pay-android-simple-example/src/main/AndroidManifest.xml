<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.clover.remotepayandroidsimpleexample">

  <uses-permission android:name="android.permission.GET_ACCOUNTS" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
  <uses-permission android:name="android.permission.INTERNET" />

  <application android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:supportsRtl="true">
    <activity android:name=".MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity android:name=".ShowMessageActivity"
        android:label="Show Message"
        android:screenOrientation="sensor"></activity>
    <activity android:name=".MakeSaleActivity"
        android:label="Make Sale"
        android:screenOrientation="sensor"></activity>
    <activity android:name=".MakeRefundActivity"
        android:label="Make Refund"
        android:screenOrientation="sensor"></activity>
  </application>

</manifest>